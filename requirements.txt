# ════════════════════════════════════════════════════════
# 数据访问（免费，无需 Google/GEE 账号，无需预下载）
# ════════════════════════════════════════════════════════

# STAC API 客户端（Planetary Computer / Earth Search）
pystac-client>=0.7.0
planetary-computer>=1.0.0     # Planetary Computer 资产签名（免费）
stackstac>=0.5.0              # STAC Items → xarray 懒加载，无需预下载

# 栅格 IO（COG 远程流式读取）
rioxarray>=0.15.0
rasterio>=1.3.0
pyproj>=3.6.0

# Open-Meteo 气候 API（完全免费，无需账号，无需 API Key）
# 直接通过标准库 urllib.request 调用，无额外依赖
# 但 requests 用于更友好的 HTTP 接口：
requests>=2.31.0

# ════════════════════════════════════════════════════════
# 数据处理
# ════════════════════════════════════════════════════════
numpy>=1.24.0
pandas>=2.0.0
xarray>=2023.1.0
dask[array]>=2023.1.0         # 并行懒加载计算

# ════════════════════════════════════════════════════════
# 地理空间分析
# ════════════════════════════════════════════════════════
geopandas>=0.14.0
shapely>=2.0.0
scipy>=1.11.0
scikit-image>=0.21.0          # 形态学操作（骨架化、中心线提取）
scikit-learn>=1.3.0           # 多元回归分析

# 趋势检验
pymannkendall>=1.4.3          # Mann-Kendall 趋势检验（Sen 斜率）

# ════════════════════════════════════════════════════════
# 可视化
# ════════════════════════════════════════════════════════
matplotlib>=3.7.0
folium>=0.14.0                # 交互式 Web 地图

# 报告生成
jinja2>=3.1.0                 # HTML 自动化报告模板

# ════════════════════════════════════════════════════════
# 开发工具
# ════════════════════════════════════════════════════════
jupyter>=1.0.0
ipykernel>=6.0.0
tqdm>=4.65.0                  # 进度条
